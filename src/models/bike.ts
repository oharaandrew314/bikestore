import {
    autoGeneratedHashKey,
    rangeKey,
    table,
    attribute
  } from '@aws/dynamodb-data-mapper-annotations'

@table(process.env.BIKES_TABLE_NAME as string)
export class Bike {

  @autoGeneratedHashKey()
  uuid: string = ''

  @attribute()
  model: string = ''

  // constructor (uuid: string, model: string) {
  //   this.uuid = uuid
  //   this.model = model
  // }
}

export class BikeData {
  constructor (public model: string) {}
}
